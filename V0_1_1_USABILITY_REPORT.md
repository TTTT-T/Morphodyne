# v0.1.1 中文化与易用性整理

日期：2026-09-24（Asia/Shanghai）。基线：`main` 的 `9be674a`。分支：`codex/v0.1.1-chinese-usability`。

## 改动

- `GodSandboxPanel.ts` / `.css`：默认界面改为中文，按世界、创建、编辑当前物体、作用与破坏修复、时间控制、环境分组；常用操作保留在主界面。物体、部件、连接等选项显示中文名称和序号，内部 ID、Blueprint/Part/Inspection JSON、损伤与连接原始状态默认收在“高级调试”。为容易误解的操作加入短说明。天气与昼夜按钮调用现有 World Environment API；蓝图校验显示中文原因。
- `main.ts` / `style.css`：场景运行信息和 Agent 手动实验控件默认折叠；移除与主面板重复的天气、昼夜按钮，并翻译调试区文案。未改变 World、Construction、Physics、Agent 的职责或核心因果逻辑。

## Mac 验证

| 检查 | 结果 |
| --- | --- |
| `npm test` | 30 个文件、109 个测试通过；42 个 TypeScript 文件的架构边界检查通过 |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过；保留原有 Rapier 大 bundle 警告 |
| `git diff --check` | 通过 |
| 浏览器 smoke（本机 Vite + Codex 浏览器） | 默认列出 5 个物体；生成箱子后成为 6 个；添加部件后该物体从 1 个部件变为 2 个；连接后从 0 个连接变为 1 个，拆开后为 0 个且显示“已拆开”，重新连接后恢复为 1 个。冲击与修复均显示成功状态。暂停后单步从第 3701 步精确推进到 3702 步；时间速度设为 0.25 倍。雨天/夜晚的环境状态显示“雨、0.0 时、日照 0.08”，恢复晴天/白天后显示“晴、12.0 时、日照 1.00”。高级调试默认折叠；输入 `{}` 检查蓝图时显示缺少材料、部件、连接数组的具体中文错误。机械结构的执行器可删除再添加（0→1），传感器可添加（0→1）。浏览器错误日志为空。 |

## 已知边界

从目录新生成的 Agent 没有默认控制源，界面已标明“未控制”。“保存蓝图”把结构 JSON 放入高级编辑区供复制，不写入文件或保存整个世界状态。Windows 验证未运行；本次任务要求 Mac 浏览器 smoke。

实现提交：`09a0bdf`（`feat(ui): make God Sandbox usable in Chinese`）。PR 链接将在创建后补入本报告。停止于 v0.1.1 评审边界，不开始 v0.2。
