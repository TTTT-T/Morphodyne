# Animal Arena v0.3 — 关节限位与身体使用实验

本次基于 `codex/animal-arena-v0.2`，按 `origin/main:NEXT_TASK.md` 的 A–F 场景验收。所有数值来自 Mac 上的 `src/tools/AnimalArenaV03.test.ts`；物理步长为 1/60 秒。**阶段仍有一项未通过：C 的支撑期足部滑移较大，不能证明移动并非由滑移主导。**

1. **spherical limit 如何表达？** `SphericalConnection.angularLimits` 是 Core 中可选的通用数组。每项给出 from-Part 局部轴、最小/最大相对角、越界刚度、外向阻尼和最大力矩。Blueprint 拒绝零轴、重复轴、非法范围与非有限参数。三轴使用与 proprioception、Actuator 相同的 Blueprint 相对坐标；机械测试体同样可用。

2. **Rapier 如何实现？** 当前 Rapier 0.20 的公开 spherical joint 接口没有运行期限位设置。Adapter 在每个固定步读取关节相对角及角速度，越界时向两端 Part 施加大小相等、方向相反的力矩，并计入 Connection load。力矩受声明的上限及单步角速度校正约束；没有改写位置、姿态或速度。通用测试体沿 x/y/z 各轴持续驱动时均保持有限角度，并可反向离开限位；A 场景正向峰值 0.267 rad、反向最低 -0.299 rad（声明范围 ±0.2 rad，有限力矩允许过冲）。这属于 **hard-ish** 物理限位，不是零过冲的求解器硬锁。

3. **与 passive compliance 如何区分？** `passiveAngular` 在范围内也持续趋向 restAngle；`angularLimits` 在范围内不产生力矩，仅越界后单侧抵抗。二者独立声明、独立计算，并共同进入关节负载估计。

4. **Leopard 主要关节范围？** 脊柱 pitch ±0.38、roll ±0.26、yaw ±0.50 rad；四个肩/髋 pitch -0.92～0.78、roll ±0.48、yaw ±0.40 rad。原有 revolute 范围保留：颈部 -0.65～0.50、下颌 -0.10～0.70 rad，尾部及膝/踝沿用各自结构限位。足部碰撞体前后半长增加至 0.28 m，前肩移至 x=0.95 m，使前肢接触位置可由真实 Part 达到。

5. **长时站立？** 单豹 600 tick，胸腔最低 0.886 m，最低 upright 0.995；按四足与步数统计的接触占比 0.811，同时四足接触占比 0.453；spherical 坐标最大绝对值 0.570 rad，消耗 4126.830 J。控制只提交关节 Actuator 信号。

6. **前进与左右转向？** 匿名目标下连续 360 tick 的胸腔净前移 0.798 m，335 tick 收到 range return，357 tick 收到足部接触感知，四个 paw 均出现接触切换。支撑时同一足部材料接触点的平面位移总和是对应胸腔位移总和的 **2.513 倍**；这提示较强的足底滑移或接触点估计误差，未满足“非滑移主导”的证明。左目标的 heading 最小 -0.286 rad、最终 -0.242；右目标最大 +0.475、最终 +0.471。肩/髋 roll 变化范围分别达 0.240/0.190 rad，yaw 达 0.670/0.620 rad。旧 240 tick 转向场景延至 260 tick，以保留原有 0.3 rad 阈值检验新的有限关节身体。

7. **两种受撞恢复？** 前左上肢受 (0,0,+35) Ns 冲量：upright 最低 0.988，180 tick 后 1.000，胸腔高 0.913 m。前右上肢受 (+20,0,-32) Ns：最低 0.982，最终 0.999，胸腔高 0.962 m。原有胸腔侧向 50 Ns 冲量回归也通过；恢复来自感知姿态、肩/髋关节信号和接触。

8. **双豹接触时如何调整？** 两只独立 Brain 各自记录 approach/interact 决策；600 tick 中实体间接触 167 tick，前肢 87、头部 42、下颌 131 tick（可同 tick 重叠）。最小胸腔间距 2.350 m；接触期间肩/髋姿态变化范围 0.550 rad，颈部 0.320，下颌 0.580。前肢 reach/brace 使用自身足部接触、膝角和匿名距离/方向；物理接触决定是否触及对方。

9. **隐藏辅助路径？** 对 Agent、Blueprint、Arena 和 Rapier adapter 扫描了对手精确坐标、身体级外力/力矩、瞬移、速度/旋转设置、直接伤害与抓附。未在正常 Arena 控制路径发现这些捷径。Adapter 的 `setTranslation`/`setRotation` 位于创建或重建描述符；`setLinvel`/`setAngvel` 仅用于结构重建时保留旧 Part 速度。新限位和 Motor 只通过关节两端力矩/Actuator 工作。

10. **下一步唯一最大的物理阻塞？** 四足步态的接触期牵引。虽然有真实目标感知、位移、足部接触切换，支撑接触点位移代理仍达 2.513 倍胸腔移动；继续调高摩擦、步频或关节目标会破坏前进、恢复或双豹接触。需要在通用接触模型与身体步态之间建立可检验的牵引指标，再改进站立相/摆动相控制；不能靠身体级推进掩盖。

## 验证与限制

- Mac：`npm test` 49 文件、191 测试通过；`npm run typecheck`、`npm run build`、`npm run check:boundaries` 通过，`git diff --check` 无问题。Vite 浏览器中观察到两只豹的实时姿态、独立目标/技能和能量变化；控制台无 error/warn。
- v0.2 的固定双豹场景现在测得头部最大接触冲量 0.448 Ns，低于其材料屈服冲量 1.2 Ns，因此真实接触没有头部形变。相应回归改为核实低于屈服时没有凭空伤害。
- Windows 未运行；本次阶段不要求 Windows 验收。
- C 的滑移判断尚未通过。当前测试记录该指标但不把“非滑移主导”伪装成已通过；360 tick 后的持续牵引也未证明。
